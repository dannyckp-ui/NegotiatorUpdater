# Negotiator Power Tool – Update Server

This repository hosts the update metadata for the **Negotiator Power Tool** desktop application.

The app checks this repository to determine whether a new version is available.  
It reads the `version.txt` file using a direct GitHub Raw link.

---

## 📌 Files

### `version.txt`
Contains the latest version number, download URL, and update notes.

Format:
